server:
  port: 8859

spring:
  application:
    name: dataAnalysis

  cloud:
    config:
      discovery:
        enabled: true
        service-id: configCenter

  profiles:
    active: local

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6751/eureka/