server:
  port: 10751

spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configCenter

  profiles:
    active: local

  application:
    name: gatewayService

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6751/eureka/