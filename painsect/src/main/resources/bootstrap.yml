server:
  port: 8851

spring:
  application:
    name: painsect

  cloud:
    config:
      discovery:
        enabled: true
        service-id: configCenter

  profiles:
    active: local

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6751/eureka/